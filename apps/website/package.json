{
  "name": "@davinci-journey/website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "lint": "astro-check && tsc -p tsconfig.json --noEmit",
    "typecheck": "astro-check && tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "astro-check && astro build && node scripts/copy-notes-assets.mjs"
  },
  "dependencies": {
    "astro": "^4.15.9",
    "mermaid": "^11.0.0",
    "rehype-stringify": "^10.0.1",
    "remark-gfm": "^4.0.1",
    "remark-parse": "^11.0.0",
    "remark-rehype": "^11.1.2",
    "unified": "^11.0.5",
    "unist-util-visit": "^5.0.0",
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
