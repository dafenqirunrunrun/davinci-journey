{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities for the local publishing workflow.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
