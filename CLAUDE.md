# Claude Code Instructions

本项目的完整产品、架构、安全、测试和 UI 规范位于根目录 `AGENTS.md`。

开始任何任务前必须先阅读 `AGENTS.md`。

固定项目名称：达芬奇的奇妙之旅。

严禁使用 `git add .`、`git reset --hard`、`git clean -fd`，严禁触碰用户未跟踪文件 `笔记—7.30.md`。

除非用户明确授权，不得执行 Git Push。
